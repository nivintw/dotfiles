# SPDX-FileCopyrightText: © 2026 Tyler Nivin
# SPDX-License-Identifier: MIT

# uv tools to install. Consumed by install.sh.
#
# Each non-comment line is the argument list passed to `uv tool install`
# (tool name plus any --with / extras). Blank lines and # comments ignored.
#
# Regenerate the human-readable view of what's installed with:
#   uv tool list --show-python --show-with --show-extras

ansible --with passlib --with jc --with jmespath --with netaddr --with ansible-lint --with-executables-from ansible-core --with-executables-from ansible-lint
ansible-dev-tools --with-executables-from molecule
checkov
commitizen
copier
jc
playwright
prek
reuse[charset-normalizer]
rumdl
# Pinned to the commit SHA behind tag v1.5.3, not the tag itself — a tag can be force-moved
# to a different commit by anyone with push access to oraios/serena; a SHA can't.
serena-agent@git+https://github.com/oraios/serena@2449313c0d7427275c4c66aedff7d4881782f713
yq
